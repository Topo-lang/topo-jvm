[project]
name = "java_data_layout"

[topo]
root = "topo/main.topo"

[build]
language = "java"
sources = ["src/main/java"]
output = "java_data_layout.jar"
output_type = "exe"

[build.java]
target_version = "21"

[optimize.data-layout]
mode = "force"

[builder]
mode = "dev"

# Benchmark scaffolding symbols are outside the .topo contract (check-only; no optimization effect).
[completeness]
ignore_constructors = true
ignore_destructors = true
ignore_patterns = [
    "app::Main",
    "app::Main::benchmarkMedianUs",
    "app::Main::run",
    "app::Particle",
]
